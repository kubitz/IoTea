from publisher import MQTT
from temp import Temp
import threading

myMQTT = MQTT()
myTemp = Temp()


def send_temp():
    temp = myTemp.read_temp()
    myMQTT.publish(temp)
    threading.Timer(3, send_temp).start()

send_temp()
