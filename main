from publisher import MQTT
from temp import Temp
import threading

myMQTT = MQTT()
myTemp = Temp()


def send_temp(period):
    temp = myTemp.read_temp()
    myMQTT.publish(temp)
    threading.Timer(period, send_temp).start()

send_temp(3)
